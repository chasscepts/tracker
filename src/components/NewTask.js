const styles = {
  container: {
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
};

export default function NewTask() {
  return (
    <div style={styles.container}>
      <h2>New Task - Coming Soon!</h2>
    </div>
  );
}
